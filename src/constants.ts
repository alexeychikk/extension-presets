// I don't know how to retrieve this using vscode API
// I could import package.json but this seems redundant
export const EXTENSION_NAME = 'extension-presets';
export const PUBLISHER = 'alex-zinkevych';
export const EXTENSION_ID = `${PUBLISHER}.${EXTENSION_NAME}`;
